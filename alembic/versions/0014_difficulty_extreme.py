"""Четвёртая ступень сложности и число дней на выход.

Три ступени расставлялись по времени и набору высоты и потому повторяли
наклейки с километрами и часами. Шкала начинает отвечать на другое —
насколько дорого стоит ошибка, — и ей нужна ступень выше «сложно»:
Аделунга с ледником и лавиной на подходе не может стоять рядом
с Большим Чимганом.

`trip_days` живёт рядом с `overnight` и уточняет его: два дня — ночёвка,
три и больше — многодневка. Без server_default намеренно: пусто значит
«однодневный выход», а не «ноль дней», и различие несущее — по нему
карточка выбирает наклейку.

ALTER TYPE ... ADD VALUE идёт внутри транзакции: на PostgreSQL 12+ это
разрешено, на проде 16. Ограничение осталось одно — новое значение
нельзя использовать в той же транзакции, поэтому саму разметку каталога
льёт отдельный прогон seed/apply_difficulty.py, а не эта миграция.

Revision ID: 0014
Revises: 0013
Create Date: 2026-08-28

"""

import sqlalchemy as sa
from alembic import op

revision = "0014"
down_revision = "0013"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.execute("ALTER TYPE difficulty ADD VALUE IF NOT EXISTS 'extreme'")
    op.add_column("places", sa.Column("trip_days", sa.Integer(), nullable=True))


def downgrade() -> None:
    op.drop_column("places", "trip_days")
    # Значение из enum обратно не убираем. В Postgres это делается только
    # пересозданием типа со снятием и возвратом колонки — операция дороже
    # и опаснее самой миграции, а лишнее значение в типе никому не мешает
