"""Сезоны сняты со всех мест: заполнять их будет игра на сайте.

Сезон был проставлен у 38 мест из 147, и проставлен по-разному: часть
руками по отчётам, часть — прикидкой. Дальше их будут заполнять живые
люди через игру на сайте, ответы проходят через одобрение владельца,
и мешать два источника в одной колонке значило бы через месяц не знать,
чему верить. Владелец выбрал чистый лист (11 сентября 2026).

Снимок прежних значений сделан отдельно, с боевого сервера, и лежит вне
репозитория: `downgrade` вернуть их не может — в самой базе после
`UPDATE` их уже нет.

Пока игра не набрала ответов, у мест нет сезона вовсе: фильтр «Только
сезонные» в приложениях не найдёт ничего, а строка «СЕЗОН ИЮЛ — СЕН»
с карточек пропадёт. Это ожидаемое состояние, а не поломка.

Revision ID: 0023
Revises: 0022
Create Date: 2026-09-11

"""

from alembic import op

revision = "0023"
down_revision = "0022"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.execute(
        "UPDATE places SET best_seasons = '{}', season_from = NULL, season_to = NULL"
    )


def downgrade() -> None:
    # Возвращать нечего: значения стёрты, а не перенесены. Восстановление —
    # из снимка, снятого перед накаткой, а не отсюда
    pass
